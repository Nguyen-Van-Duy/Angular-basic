import { AboutDirective } from './about.directive';

describe('AboutDirective', () => {
  it('should create an instance', () => {
    const directive = new AboutDirective();
    expect(directive).toBeTruthy();
  });
});
